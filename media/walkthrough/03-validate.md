### Validate and improve SQL

Use built-in checks before running large scripts:

- **Lint SQL (On-Demand)** for safety/performance rules
- **Validate SQL** for parser-based syntax/semantic checks
- Apply available **Quick Fixes** from the lightbulb menu

[Lint SQL now](command:netezza.lintSql)
