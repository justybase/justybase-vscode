#!/bin/sh

set -eu

SCRIPT_DIR=$(CDPATH= cd -- "$(dirname -- "$0")" && pwd)
if command -v mvn >/dev/null 2>&1; then
  exec mvn "$@"
fi

echo "Maven is required to build the Access bridge. Install Maven 3.9.11+ or provide mvn on PATH." >&2
exit 1
